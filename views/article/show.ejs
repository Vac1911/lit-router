<!DOCTYPE html>
<html>

<head>
    <title>
        <%= article.title %>
    </title>
    <meta charset="UTF-8" />
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css?family=Playfair+Display:700,900&amp;display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
    <link href="/styles.css" rel="stylesheet">
    <script defer src="/index.js"></script>
</head>

<body style="visibility: hidden; background-color: var(--dark-1); position: relative">
    <div id="app">
        <%- include(root+'/include/navigation'); -%>
        <article-view>
            <div class="container p-5">
                <div class="p-4 rounded" style="background-color: var(--dark-2);">
                    <h1 class="mb-0">
                        <%= article.title %>
                    </h1>
                    <div class="mb-3 text-muted">
                        <%= DateTime.fromSeconds(article.created_at).toFormat('LLL dd yyyy') %>
                    </div>
                    <p><%- article.body.replaceAll("\n", '</p><p>' ) %></p>
                </div>
            </div>
        </article-view>
    </div>
</body>

</html>